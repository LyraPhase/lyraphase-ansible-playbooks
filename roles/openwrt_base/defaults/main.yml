---
# openwrt_base: entry point for defaults

# Bootstrap IP to set (static)
#openwrt_base_bootstrap_ip: 192.168.1.2
#openwrt_base_bootstrap_netmask: 255.255.255.0
#openwrt_base_bootstrap_dhcp_proto: 'static'

# Dumb AP mode: use DHCP for br-lan
openwrt_base_bootstrap_dhcp_proto: "dhcp"

openwrt_base_sysctl_d:
  local:
    - "# ": "local sysctl settings can be stored in this directory"
